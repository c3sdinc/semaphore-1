---
- hosts: all
  become: true
  pre_tasks:

  - name: install updates for Debian
    tags: always
    ansible.builtin.apt:
      update_cache: yes
      install_recommends: yes
      force_apt_get: yes
      upgrade: yes
      autoclean: yes
      autoremove: yes
    when: ansible_distribution == "Debian"
