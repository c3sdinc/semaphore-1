---
- hosts: all
  become: true
  pre_tasks:

  - name: install updates for Debian
    tags: always
    ansible.builtin.apt:
      update_cache: yes
      only_upgrade: yes
      autoremove: yes
    when: ansible_distribution == "Debian"
